import LogoHeader from "./LogoHeader";

const Header = () => {
  return (
    <header>
      <nav className="container">
        <LogoHeader />
      </nav>
    </header>
  );
};

export default Header;
